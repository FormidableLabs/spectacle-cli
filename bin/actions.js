'use strict';

const launchDevServer = async ({ port, title, mdxFilePath, themeFilePath }) => {
  console.log('TODO HERE DEV SERVER', {
    port,
    title,
    mdxFilePath,
    themeFilePath
  });
};

module.exports = {
  launchDevServer
};
