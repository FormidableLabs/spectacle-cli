import Slide from './spectacle-component';

# MDX + Babel + Spectacle JS Slide 👋

This example uses `mdx` + `babel` custom extensions for the React components we import + a custom JS slide using `spectacle`.

- four
- five
- six

---

## A custom JS Spectacle slide!

<Slide />

Notes: here are some more notes.
